import React from 'react';

const Categories = ({quiz}) => {
    console.log(quiz);
    const {logo, name, total} = quiz;

    return (
        <div>
            
        </div>
    );
};

export default Categories;